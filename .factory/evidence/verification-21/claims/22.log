
> mtd-evidence-rail@1.0.0 test:deployment-topology
> bash scripts/test-deployment-topology.sh

Live release guard regressions PASS — unsafe topology and stale identity fail; verification 20 uses the exact factory candidate instead of a stale release manifest.
Verification 16 self-repair integration PASS — the exact generic revision requested a durable replacement and exited without serving.
@claim:production-topology PASS — an unsafe three-replica payload renders as one durable replica, and a later generic rollout requests that topology before it can serve
